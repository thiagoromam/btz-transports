@model int?
@{
    ViewBag.Title = Model != null ? "Editar abastecimento" : "Novo abastecimento";
}

@section scripts {
    @Scripts.Render("~/bundles/api/abastecimentos")
    @Scripts.Render("~/bundles/abastecimento/editar")
}

<div ng-controller="EditarController" ng-init="carregar(@Model)">
    <form ng-submit="salvar()">
        @*<div class="form-group">
            <label>Nome</label>
            <input type="text" class="form-control" ng-model="abastecimento.nome" maxlength="50" required />
        </div>
        <div class="form-group">
            <label>CPF</label>
            <input type="text" class="form-control" ng-model="abastecimento.cpf" maxlength="11" required />
        </div>
        <div class="form-group">
            <label>CNH</label>
            <input type="text" class="form-control" ng-model="abastecimento.cnh" maxlength="10" required />
        </div>
        <div class="form-group">
            <label>CNH</label>
            <input type="date" class="form-control" ng-model="abastecimento.dataDeNascimento" required />
        </div>
        <div class="form-group">
            <label>Situação</label> <br />
            <div class="form-check form-check-inline" ng-repeat="e in Enum.statusDoMotorista.options">
                <input class="form-check-input" type="radio" ng-model="abastecimento.status" id="status_{{e.name}}" ng-value="e.value" />
                <label class="form-check-label" for="status_{{e.name}}" ng-bind="e.display"></label>
            </div>
        </div>*@

        <div class="mt-5">
            <button class="btn btn-primary">Salvar</button>
            <a class="btn btn-secondary" href="@Url.Action("Index")">Cancelar</a>
        </div>
    </form>
</div>